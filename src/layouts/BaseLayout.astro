---
import { Image, getImage } from 'astro:assets';
import SocialLinks from '../components/SocialLinks.astro';
import avatar from '../assets/profile/colin_emoji.png';
import { SHOW_NOTES } from "astro:env/server";
import { randomEmoji } from '../lib/greetings';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  type?: 'website' | 'article';
  hideChrome?: boolean;
}

const { title, description, image, type = 'website', hideChrome = false } = Astro.props;
const siteTitle = 'Colin McCloskey';
const pageTitle = title ? `${title} — ${siteTitle}` : siteTitle;
const pageDescription = description ?? 'Personal site of Colin McCloskey. Bearded, computery, nerd person.';

const fallbackOgImage = await getImage({ src: avatar, width: 512, height: 512, format: 'jpeg' });
const ogImagePath = image ?? fallbackOgImage.src;
const ogImageUrl = new URL(ogImagePath, Astro.site).href;
const pageUrl = new URL(Astro.url.pathname, Astro.site).href;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <meta name="apple-mobile-web-app-title" content="McColin" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <meta name="description" content={pageDescription} />
    <meta property="og:type" content={type} />
    <meta property="og:site_name" content={siteTitle} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={pageDescription} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:url" content={pageUrl} />
    <link
      rel="preconnect"
      href="https://fonts.googleapis.com"
    />
    <link
      rel="preconnect"
      href="https://fonts.gstatic.com"
      crossorigin
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,600;1,400;1,600&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    {!hideChrome && (
      <header>
        <div class="page-wrap header-inner">
          <a href="/" class="site-name">
            <Image src={avatar} alt="An emoji representation of Colin McCloskey, one of the most handsome people on the Internet." width={48} height={48} class="site-avatar" />
            Colin McCloskey
          </a>
          <nav>
            <a href="/about">About</a>
            <a href="/resume">Resume</a>
            { SHOW_NOTES ? <a href="/notes">Notes</a> : null }
          </nav>
        </div>
      </header>
    )}

    <main>
      <slot />
    </main>

    {!hideChrome && (
      <footer>
        <div class="page-wrap footer-inner">
          <nav class="footer-nav">
            <a href="/">Home</a>
            <a href="/about">About</a>
            <a href="/resume">Resume</a>
            { SHOW_NOTES ? <a href="/notes">Notes</a> : null }
          </nav>
          <span class="footer-copy">&copy; {new Date().getFullYear()} Colin McCloskey</span>
          <span class="footer-emoji" title={`Built ${new Date().toLocaleString('en-US', { timeZone: 'America/New_York', dateStyle: 'long', timeStyle: 'short' })}, Philadelphia local time`}>{ randomEmoji(3).join(' ') }</span>
        </div>
      </footer>
    )}
  </body>
</html>

<style is:global>
  @import '../styles/global.css';
</style>

<style>
  header {
    background-color: var(--color-header-bg);
    border-bottom: 1px solid var(--color-border);
    padding-block: 1rem;
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .site-name {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    color: var(--color-text);
    white-space: nowrap;
  }

  .site-avatar {
    width: 48px;
    height: 48px;
    border-color: var(--color-border);
    border-radius: 50%;
    border-style: solid;
    border-width: 1px;
    object-fit: cover;
    flex-shrink: 0;
  }

  nav {
    display: flex;
    gap: 1.5rem;
  }

  nav a {
    color: var(--color-text-muted);
    text-decoration: none;
    font-size: 0.9rem;
    transition: color 0.15s;
  }

  nav a:hover {
    color: var(--color-text);
  }

  footer {
    background-color: var(--color-header-bg);
    border-top: 1px solid var(--color-border);
    margin-top: 4rem;
    padding-block: 1.5rem;
  }

  .footer-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.85rem;
    color: var(--color-text-muted);
    text-align: center;
  }

  .footer-nav {
    display: flex;
    gap: 1.5rem;
  }

  .footer-nav a {
    color: var(--color-text-muted);
    text-decoration: none;
    font-size: 0.85rem;
    transition: color 0.15s;
  }

  .footer-nav a:hover {
    color: var(--color-text);
  }

  .footer-copy {
    font-size: 0.8rem;
  }

  .footer-emoji {
    font-size: 22pt;
  }
</style>
